//SPDX-License-Identifier: Unlicense
pragma solidity ^0.8.0;

import "hardhat/console.sol";

contract Echo {
    
    event LogMessage(address indexed reciever, string message);
    event LogIdentity(address indexed communicationAddress, address indexed identityAddress);

    
}
